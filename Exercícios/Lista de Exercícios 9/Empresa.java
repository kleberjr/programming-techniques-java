import java.util.*;
import lista9classes.*;
import lista9interfaces.*;

public class Empresa {
	Scanner input = new Scanner(System.in).useLocale(Locale.US);
	
	int qtdProduto = input.nextInt();
	int qtdCliente = input.nextInt();
	int qtdServico = input.nextInt();
	
	Classificavel cls;
	
	/* Loop para cadastrar os produtos de acordo com a quantidade informada */
	
	/* Loop para cadastrar os clientes de acordo com a quantidade informada */
	
	/* Loop para cadastrar os serviços de acordo com a quantidade informada */
	
}
